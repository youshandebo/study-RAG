fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
email-validator>=2.0   # auth 注册 EmailStr 校验（pydantic[email]）
httpx>=0.27
python-multipart>=0.0.9
aiosqlite>=0.20   # 默认持久化（SQLite 单容器模式；配置 POSTGRES_DSN 时可不装）

# ---- 可选重型依赖（生产部署按需启用，启用对应配置项时必须安装）----
# qdrant-client>=1.9          # 配置 QDRANT_URL 时必须（检索候选真正走 Qdrant payload filter）
# sqlalchemy[asyncio]>=2.0    # 配置 POSTGRES_DSN 时必须
# asyncpg>=0.29               # 同上（Postgres 异步驱动）
# celery[redis]>=5.3
# redis>=5.0
# minio>=7.2
# faster-whisper>=1.0
